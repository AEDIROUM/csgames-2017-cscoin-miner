libastar_lib = library('astar', ['src/astar.c', 'src/astar_heap.c'])
libastar = declare_dependency(link_with: libastar_lib,
                              include_directories: include_directories('src'))
